{
  "name": "openspec-dashboard-root",
  "version": "0.1.0",
  "private": true,
  "description": "OpenSpec Dashboard Workspace",
  "workspaces": [
    "client",
    "server",
    "cli"
  ],
  "scripts": {
    "postinstall": "chmod +x node_modules/node-pty/prebuilds/*/spawn-helper 2>/dev/null || true",
    "dev": "concurrently -n \"client,server\" -c \"cyan,green\" \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server && npm run build -w cli",
    "test": "npm run test -w server && npm run test -w client",
    "test:e2e": "TEST_MODE=true playwright test -c e2e/playwright.config.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@playwright/test": "^1.44.1",
    "typescript": "^5.4.5"
  }
}
