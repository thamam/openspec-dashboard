{
  "name": "openspec-dashboard-root",
  "version": "0.1.0",
  "private": true,
  "description": "OpenSpec Dashboard Workspace",
  "workspaces": [
    "client",
    "server",
    "cli"
  ],
  "scripts": {
    "dev": "concurrently -n \"client,server,cli\" -c \"cyan,green,yellow\" \"npm run dev -w client\" \"npm run dev -w server\" \"npm run dev -w cli\"",
    "build": "npm run build -w client && npm run build -w server && npm run build -w cli",
    "test": "npm run test -w server && npm run test -w client",
    "test:e2e": "playwright test -c e2e/playwright.config.ts"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "@playwright/test": "^1.44.1",
    "typescript": "^5.4.5"
  }
}
